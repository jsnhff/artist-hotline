# VoIP and audio processing (must be first - has strict version requirements)
vocode==0.1.108

# Core dependencies for production (compatible with vocode)
python-dotenv
twilio
httpx
websockets
openai>=1.0.0
numpy>=1.24.0

# Audio processing for WebSocket streaming (MP3 to µ-law conversion)
pydub>=0.25.1
audioop-lts>=0.2.1  # Replacement for audioop in Python 3.13+

# Optional dependencies for development/testing
# Simple TTS fallback
pyttsx3>=2.90

# Coqui TTS Test System Dependencies (install manually when USE_COQUI_TEST=true)
# These are heavy dependencies that can cause Railway build timeouts
# pip install TTS>=0.22.0 torch>=2.0.0 faster-whisper>=1.0.0

# Development tools (not needed for production)
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.7.0