vocode==0.1.108
python-dotenv
twilio
httpx
websockets
numpy>=1.24.0

# Coqui TTS Test System Dependencies (optional - only needed when USE_COQUI_TEST=true)
# Install these manually when testing: pip install TTS torch faster-whisper
# TTS>=0.22.0
# faster-whisper>=1.0.0
# torch>=2.0.0
# Note: audioop is built-in to Python but will be deprecated
# For future compatibility, consider: audioop-lts